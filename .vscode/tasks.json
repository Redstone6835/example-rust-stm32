{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build ELF",
            "type": "shell",
            "command": "cargo build && cd xtask && cargo run && cd ..",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$rustc"
            ],
            "presentation": {
                "reveal": "always"
            }
        }
    ]
}
